1- Agregado en:
GameEngine.scala 
  /** 
   * Mueve el punto una unidad hacia arriba.
   * 
   * @return Nuevo punto con y decrementado en 1
   */
  def moveUp(): Point = copy(y = y - 1)

2- Modificado en:
Point.scala

       val rotatedStone = currentStone.rotate(clockwise)
POR -->
 	val rotatedStone = if (clockwise) currentStone.rotateRight() else 	currentStone.rotateLeft()

3- Agregado en:

Stone.scala 
  /** 
   * Mueve la pieza un espacio hacia arriba.
   * 
   * @return Nueva pieza con la posición actualizada
   */
  def moveUp(): Stone = copy(points = points.map(_.moveUp()))


--------------------
1. correr: sbt doc
2: cd target/scala-3.3.1/api; python -m http.server 8000

